[project]
name = "medha-backend"
version = "0.2.0"
description = "MEDHA backend — constraint-negotiating multi-agent DevOps automation (Phases 0–12)"
requires-python = ">=3.11"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
pythonpath = ["."]

services:
  database:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
  backend:
    build: .
    ports:
      - "8080:8080"
    depends_on:
      - database
  frontend:
    image: medha/frontend:local
    ports:
      - "3000:3000"
    depends_on:
      - backend
